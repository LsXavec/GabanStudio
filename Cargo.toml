[workspace]
resolver = "2"
members = ["m0_spike", "crates/anim-core"]

# Fast iteration: our crates compile quick, dependencies stay optimized
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[profile.release]
opt-level = 3
lto = "thin"
